import { LigthboxDirective } from './ligthbox.directive';

describe('LigthboxDirective', () => {
  it('should create an instance', () => {
    const directive = new LigthboxDirective();
    expect(directive).toBeTruthy();
  });
});
